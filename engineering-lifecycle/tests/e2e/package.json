{
  "name": "engineering-lifecycle-e2e",
  "private": true,
  "description": "Browser coverage for the generated project dashboard, the plugin's only browser-facing artifact.",
  "scripts": {
    "fixture": "python -B ../../scripts/build-dashboard-fixture.py --out .fixture",
    "test": "playwright test",
    "test:install": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
